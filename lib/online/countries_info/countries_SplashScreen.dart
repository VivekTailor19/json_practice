import 'package:flutter/material.dart';

class Countries_SplashScreen extends StatefulWidget {
  const Countries_SplashScreen({Key? key}) : super(key: key);

  @override
  State<Countries_SplashScreen> createState() => _Countries_SplashScreenState();
}

class _Countries_SplashScreenState extends State<Countries_SplashScreen> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(),
    );
  }
}
